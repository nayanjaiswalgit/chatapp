#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
using namespace std;


int main() {
 int count = 1 ;
    string ans = "";
    string a;
    cin>>a;
    bool w = false ; 
    int q = 0;
    for(int i = 1 ; i<a[i] ; i++){
        if(a[i]==a[i-1]){
            q++;
            w = true;
        }
        else {
            q  = 0;
            
        }
    }
    if(q==0 && !w){
        cout<<a;
        return 0 ;
    }
    
    
    
    
    
    
    
    
    
    for(int i = 1 ; i<a[i];i++){
        if(a[i]==a[i-1]){
            count++;
        }
        else {
            ans+=a[i-1];
            int t = count;
            ans+= to_string(t);
            count = 1 ;
        }
    }
    ans+=a[a.length()-1];
    ans+= to_string(count);
    cout<<ans;
}
